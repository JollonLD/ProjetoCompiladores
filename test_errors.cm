/* Programa com erros semânticos para teste */

int main(void) {
    int x;
    void @y;        /* ERRO: variável não pode ser void */
    int arr[10];

    x = 5;
    y = 10;        /* ERRO: y é void */
    arr = 20;      /* ERRO: não pode atribuir a array completo */

    z = x + 1;     /* ERRO: z não foi declarado */

    if (arr) {     /* ERRO: condição deve usar array indexado */
        x = arr;   /* ERRO: uso de array sem indexação */
    }

    x = arr[x];    /* OK */

    return x;
}